# AI 工作文档

## 任务 0：基线与运行确认

### 修改内容

- 确认分支 `feat/yaorz`，基线为 `1f8f4fd`，已包含 `origin/main` 的 `b245aea` 合并内容。
- 保留用户已有的 `frontend/package-lock.json` 和评审文档，不覆盖未授权工作区改动。

### 结果

- 前端构建、前端页面、后端根路径、OpenAPI、元数据、图谱和搜索接口均正常。

## 任务 1：星图视觉层级与卡片布局（1.1、1.5、1.6、1.7）

### 修改内容

- `frontend/src/graphExperience.tsx`
  - 将 `Clear selection`、`Open search library` 移至图谱筛选侧栏，与 `Graph controls`、`Downloading table` 同级。
  - 保留 `Reset layout` 在 `Graph controls` 菜单中，未改变操作逻辑。
- `frontend/src/styles/app.css`
  - 提高 Dataset 下拉菜单和侧栏 Dataset 触发器的文字对比度。
  - 增加关系卡网格收缩、长文本断行、操作列对齐和窄屏元数据布局，避免元素重叠。

### 结果

- 前端构建通过；桌面和窄屏截图检查通过。
- Dataset 文字、同级操作入口、关系卡下载按钮和关系卡内容均通过人工确认。

## 任务 2：星图缩放、选择与拖拽交互（1.2、1.3、1.4、1.8）

### 修改内容

- `frontend/src/graphExperience.tsx`
  - 增加图谱滚轮/触控板缩放、缩放比例、加减和重置控件。
  - 增加图谱空白清除选择和 Escape 清除方式。
  - 使用 SVG 屏幕坐标矩阵换算拖拽位移，并按缩放比例修正节点拖拽。
  - 拖拽期间合并相机更新，关闭过渡和节点滤镜；移除侧栏 `Clear highlights` 入口。
- `frontend/src/styles/app.css`
  - 增加缩放控件以及拖拽期间的无过渡、无滤镜样式。

### 结果

- 前端构建通过；桌面与窄屏图谱截图、事件绑定和坐标换算复核通过。
- `1.2` 缩放、`1.3` 空白清除、`1.4` 横向拖拽位移通过人工确认。
- `1.8` 拖拽闪烁仍存在，按要求暂缓处理并作为遗留问题记录。

## 任务 3：搜索结果选择与队列状态一致性（2.1、2.2、2.3）

### 修改内容

- `frontend/src/components/SearchResult.tsx`
  - 增加选中结果的金色边框、内侧标记和浅色背景。
  - 使用现有下载图标显示加入/移出队列状态；持续显示 `Queued`，不增加 `Added`/`Removed` 中间状态。
  - 补充队列操作标题和无障碍标签。
- `frontend/src/pages/SearchPage.tsx`
  - 将每个结果的队列状态从页面状态传入，保持列表状态一致。
- `frontend/src/App.tsx`
  - 复用既有队列数据源；搜索结果为空时清除旧选中项，搜索或筛选变化后自动同步有效详情。
- `frontend/src/styles/inner-pages.css`
  - 增加选中与已入队状态样式，并修正浅色选中背景下的文字对比度。

### 结果

- 前端构建、桌面截图和交互复核通过。
- 列表选中项、右侧详情、队列按钮和下载队列状态同步通过人工确认。

## 任务 4：搜索侧栏、详情滚动与内容适配（2.4、2.5、2.6、2.7、2.8）

### 修改内容

- `frontend/src/pages/SearchPage.tsx`
  - 移除页面标题区域重复的 `Clear filters`，保留左侧筛选面板入口。
- `frontend/src/styles/inner-pages.css`
  - 为桌面端 Searching filters 和详情面板增加顶栏下方 sticky 定位、最大高度、独立纵向滚动和滚动边界。
  - 使用 `overflow-x: clip` 避免外层容器阻断 sticky；详情/筛选面板关闭横向滚动。
  - 提高搜索统计摘要和详情字段标签对比度。
  - 为详情字段和详情描述增加可收缩列与强制断行，长 SMILES/标识符在面板内换行。
  - 中小屏恢复页面自然滚动，移动端恢复固定侧栏导航。

### 结果

- 前端构建通过；桌面 1600×900 与窄屏 375×812 截图检查通过。
- 页面滚动到 `700px` 时两栏顶部稳定在顶栏下方 `80px`。
- 详情面板和筛选面板独立滚轮复核通过，窗口滚动未被锁死。
- `CHEBI:131857` 长 SMILES 的详情描述和字段行均换行，详情面板无横向溢出。
- 标题区重复清除入口已移除，统计摘要对比度通过人工确认。

## 任务 5：第 1、2 大类全量回归

### 修改内容

- 本项不新增代码，仅对第 1、2 大类及共享组件进行回归验证。
- 为主应用补充 favicon 资源，消除浏览器默认图标请求的 404。
- 将图谱滚轮缩放改为 `passive: false` 的原生监听，保留 `preventDefault()` 和原有缩放算法。

### 结果

- `npm.cmd run build` 通过，TypeScript 检查和 Vite 生产构建均成功。
- 前端、后端根路径、OpenAPI、元数据、图谱、搜索和 favicon 请求均返回 `200`；`git diff --check` 通过。
- 首页图谱加载 120 个节点、130 个关系组；Dataset 菜单、Graph controls、同级操作入口和缩放控件正常。
- 图谱滚轮缩放从 `100%` 正常变为 `143%`，且无 passive listener 警告；`1.8` 拖拽闪烁仍按要求作为遗留问题跳过。
- 搜索页选中同步、队列加入/移出、无结果清空、类型筛选、长 SMILES 换行和详情队列状态均正常；未出现 `Added`/`Removed` 中间状态。
- 桌面端滚动到 `700px` 时两栏稳定在顶栏下方 `80px`，详情与筛选面板可独立滚动且窗口滚动未锁死。
- 移动端 `375×812` 页面宽度无横向溢出，侧栏和详情按自然页面流排列；桌面与移动端视觉检查通过。
- 全新页面加载及交互回归未发现控制台异常、网络错误或未处理运行时异常。
